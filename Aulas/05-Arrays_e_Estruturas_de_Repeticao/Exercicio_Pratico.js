// 1) Crie um programa que dado um número imprima a sua tabua.

const tamanho = [];

tamanho.push(10);
const numero = 3;

for (let i = 1; i <= tamanho; i++) {
       const total = numero * i ;
console.log( i + ' x ' + numero + ' = ' + total);  
}